Because AuditLogger calls getMorphClass(), add Announcement to the SAME existing
Relation::enforceMorphMap() map:

use App\Models\Communication\Announcement;

Relation::enforceMorphMap([
    // keep all existing aliases...

    'announcement' => Announcement::class,
]);

Verify:

php artisan tinker

(new \App\Models\Communication\Announcement())
    ->getMorphClass();

Expected:

"announcement"
